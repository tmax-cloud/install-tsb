apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: template-service-broker
  name: template-service-broker
  namespace: ${YOUR_NAMESPACE}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: template-service-broker
  template:
    metadata:
      labels:
        app: template-service-broker
    spec:
      serviceAccountName: tsb-sa
      containers:
      - image: tmaxcloudck/tsb:0.0.1
        name: tsb
        imagePullPolicy: Always